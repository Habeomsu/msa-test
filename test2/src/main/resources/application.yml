spring:
  application:
    name: test2
  profiles:
    active: dev
  config:
    import: optional:configserver:http://localhost:8071

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8082



